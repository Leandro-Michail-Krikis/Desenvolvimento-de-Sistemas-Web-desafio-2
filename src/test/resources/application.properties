spring.application.name=desafio2-test

# PostgreSQL for tests
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:desafio2}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Use Docker Compose during tests too
spring.docker.compose.enabled=true
spring.docker.compose.skip.in-tests=false

# JWT (same secret as main for tests)
jwt.secret=ZGVzYWZpbzItand0LXNlY3JldC1rZXktbXVpdG8tc2VndXJvLWUtbG9uZ28tMjU2Yml0cw==
jwt.expiration=86400000
